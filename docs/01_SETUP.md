# Setup & Run Guide

## Prerequisites
- Git
- Go (for `auth-gateway`)
- Python 3.10+ (for scripts / ML services)
- Node.js 18+ (for dashboard)

## 1) Clone & checkout
```bash
git clone <repo_url>
cd aadhar-next
git checkout aj_works


